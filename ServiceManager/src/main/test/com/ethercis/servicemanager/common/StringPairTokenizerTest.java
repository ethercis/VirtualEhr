//Copyright
package com.ethercis.servicemanager.common;

import junit.framework.TestCase;

public class StringPairTokenizerTest extends TestCase {

   //TODO: complete test
    public void testTokenize() {
//        String test = (args.length > 0) ? args[0] : "/node/heron/client/\"joe/the/great\"";
//        String separator = (args.length > 1) ? args[1] : "/";
//        String quotechar = (args.length > 2) ? args[2] : "\"";
//
//        System.out.println("From '" + test + "' we get");
//      /*
//      {
//         System.out.println("with split():");
//         String[] result = split(test, separator, quotechar);
//         for (int i=0; i<result.length; i++)
//            System.out.println("'" + result[i] + "'");
//      }
//      */
//
//        {
//            System.out.println("with parseLine():");
//            String[] result = StringPairTokenizer.parseLine(test, separator.charAt(0), quotechar.charAt(0), true);
//            for (int i=0; i<result.length; i++)
//                System.out.println("'" + result[i] + "'");
//        }
//
//        {
//            System.out.println("\nTesting with quotes:\n");
//            String[] nextLines = { "com.ethercis.protocol.soap.SoapDriver,\"classpath=xerces.jar:soap.jar,all\",MAXSIZE=100,a=10,\"b=", "20\",c=30" };
//            Map map = StringPairTokenizer.parseLine(nextLines, StringPairTokenizer.DEFAULT_SEPARATOR, StringPairTokenizer.DEFAULT_QUOTE_CHARACTER, StringPairTokenizer.DEFAULT_INNER_SEPARATOR, true, false, true);
//            java.common.Iterator it = map.entrySet().iterator();
//            while (it.hasNext()) {
//                Map.Entry entry = (Map.Entry)it.next();
//                System.out.println(entry.getKey() + "=" + entry.getValue());
//            }
//        }
    }

}